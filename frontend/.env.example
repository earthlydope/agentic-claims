# Leave empty for local `npm run dev` (Vite proxies /api → :8099).
# On Vercel, set this to the Cloud Run origin with no trailing slash.
# Example: https://agentic-claims-api-xxxxx-ew.a.run.app
VITE_API_BASE=
